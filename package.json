{
  "name": "auto-bot-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:api": "node api/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
